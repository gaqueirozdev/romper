import { getColor } from "../../assets/helpers/palette";

export const styledAuthenticationBox = {
  backgroundColor: getColor('gray-dark'), 
  borderRadius: '1.2rem', 
  display: 'flex',
  alignItems: 'center', 
  justifyContent: 'center',
  flexDirection: 'column',
  padding: '2.6rem 1.6rem'
}