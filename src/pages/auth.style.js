import { getColor } from "../assets/helpers/palette";

export const styledAuthPageTitle = {
  marginBottom: '2.4rem',
  textAlign: 'center',
  fontWeight: 'bold',
  fontSize: '3.6rem',
  color: getColor('white')
}